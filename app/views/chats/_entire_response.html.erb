<div class="chat-image-container">
  <% if chat.messages.any? %>
    <div id="<%= dom_id(assistant_message) %>">
      <%= cl_image_tag @chat.messages.last.image.blob.key %>
    </div>
  <% end %>
</div>
<div id="messages" class='mt-3'>
  <%= assistant_message.content %>
</div>