<div class="image mt-3 d-flex justify-content-center">
  <% if chat.messages.any? %>
    <%= render 'messages/image', message: chat.messages.last %>
  <% end %>
</div>
<div id="messages" class='mt-3'>
  <%= @chat.messages.where(role: 'assistant').last.content %>
</div>
